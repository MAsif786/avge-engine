"""
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────MCP server — the primary LLM-facing interface for AVGE Engine.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────§12.3: The 4 M0b tools are registered as MCP tools, with input schemas
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────generated from the Tool Schema Registry (schemas/*.json) — single source
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────of truth, no duplicate schema maintenance.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────The server advertises __tool_set_version__ so LLM clients can detect
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────stale cached tool lists after an engine upgrade.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────"""
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────from __future__ import annotations
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────import sys
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────from mcp.server.fastmcp import FastMCP
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────from avge_engine import __version__, __tool_set_version__
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────from avge_engine.schema_registry import validate_input
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────from avge_engine.scene import SceneGraph, CurveConstraints, Style
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────from avge_engine.renderer import svg_serialize, render_preview_base64
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────from avge_engine.geometry import compute_bounds
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Global scene graph ──────────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────_graph: SceneGraph | None = None
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────_active_doc: str | None = None
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def _get_graph() -> SceneGraph:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    global _graph
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if _graph is None:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        _graph = SceneGraph()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    return _graph
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def _resolve_doc(document_id: str | None = None) -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Resolve document_id from explicit value or active session."""
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    global _active_doc
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if document_id:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return document_id
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if _active_doc is None:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        raise RuntimeError("No active document. Call create_document first.")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    return _active_doc
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Design guidelines resource ──────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────DESIGN_GUIDELINES_PATH = None  # resolved on first access
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def _load_design_guidelines() -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    global DESIGN_GUIDELINES_PATH
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    from pathlib import Path
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if DESIGN_GUIDELINES_PATH is None:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        DESIGN_GUIDELINES_PATH = Path(__file__).resolve().parent.parent / "design-guidelines.md"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if DESIGN_GUIDELINES_PATH.exists():
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return DESIGN_GUIDELINES_PATH.read_text()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    return "# Design Guidelines\n\n(See design-guidelines.md — file not found on this server.)"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Tool descriptions with §4.5d smoothness guidance ───────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────SMOOTHNESS_GUIDANCE = """
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────Smoothness guidance (per-region):
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────  - Geometric/polygonal (houses, stars, rectangles): smoothness=0.0–0.1
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────  - Mixed rigid/organic (cup body, tree trunk, saucer): smoothness=0.2–0.5
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────  - Organic/curved (foliage, faces, circles): smoothness=0.6–0.8
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────  - Smoothness=0.5 is the default — adjust per-region per the above.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────"""
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Server ─────────────────────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────mcp = FastMCP(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    "AVGE Engine",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    instructions=f"""AI-Native Vector Graphics Engine (AVGE) — M0b Production Build.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────Tool set version: {__tool_set_version__}
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────Coordinates are normalized 0.0–1.0 where (0,0) = top-left, (1,1) = bottom-right.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────📏 **Proportion rule**: every object's canvas footprint must match its
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────real-world size. A desk is ~90%+ of canvas width; headphones on it are
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────~15–25%; a water bottle is ~6–8%. If an item sits on a surface, its
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────width should be ≤ ¼ of the supporting surface. See resource
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────avge://skill/design-guidelines Rule 5 for full guidance.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────{SMOOTHNESS_GUIDANCE}
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────📝 **Workflow — edit the same document, never rebuild from scratch:**
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────1. `create_document` once with a `name` for your scene.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────2. `create_region` to add shapes.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────3. `style_objects` to recolor existing regions — no need to delete and recreate.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────4. `describe_scene` / `render_preview` to check and iterate.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────5. `checkpoint` before risky edits so `restore` can undo multiple steps.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────6. `delete_region` to remove unwanted geometry — avoids full rebuilds.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────👤 **Character design (see avge://skill/design-guidelines Rule 8):**
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────Before drawing any human character, first ask: what style (anime/realistic/cartoon/chibi)?
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────Each style has different head-to-body ratios, eye placement, and feature detail.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────Build in order: face → hair → eyes → brows → nose → mouth → ears → neck + torso → arms → legs.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────Use describe_scene between steps to verify positions. Checkpoint before risky edits.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────""",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── MCP Resource: full design skill (registered after mcp is defined) ─
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────@mcp.resource(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    uri="avge://skill/design-guidelines",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    name="AVGE Design Guidelines",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    description="Full design skill: aesthetic conventions for two-tone shading, "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "stroke-weight hierarchy, palette selection, grounding, composition, "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "and style-register matching. Referenced by tool descriptions.",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    mime_type="text/markdown",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────async def design_guidelines_resource() -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Return the full design guidelines markdown document."""
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    return _load_design_guidelines()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Tool 1: create_document ────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────@mcp.tool(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    name="create_document",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    description="Create a new canvas. Must be called first — call once per scene, then use create_region/style_objects to edit the same document incrementally. Never rebuild from scratch.",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def create_document(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    width: int = 1000,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    height: int = 1000,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    unit: str = "px",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    background: str = "#FFFFFF",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────) -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Create a new vector document / canvas.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    Args:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        width: Canvas width in pixels (100–4000, default 1000).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        height: Canvas height in pixels (100–4000, default 1000).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        unit: Unit of measurement ("px", "in", "mm", "cm").
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        background: Background hex color (default "#FFFFFF").
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    # Schema-level validation
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    errs = validate_input("create_document", {
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        "width": width, "height": height, "unit": unit, "background": background,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    })
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if errs:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return f"Validation error: {'; '.join(errs)}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    scene = _get_graph()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        doc = scene.create_document(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            width=max(100, min(width, 4000)),
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            height=max(100, min(height, 4000)),
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            unit=unit,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            background=background,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        )
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        global _active_doc
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        _active_doc = doc.id
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        name_str = f" name='{doc.name}'" if doc.name else ""
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return (
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            f"Document created: id={doc.id}{name_str}, "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            f"{doc.width}x{doc.height} {doc.unit}, "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            f"background={doc.background}\n\n"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            f"📌 Use this document_id=\"{doc.id}\" in subsequent tool calls.\n"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            f"💡 Tip: before styling, see resource avge://skill/design-guidelines "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            f"— flat single-tone fills on every region is a common miss."
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        )
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except RuntimeError as e:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return f"Error: {e}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Tool 2: create_region ─────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────@mcp.tool(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    name="create_region",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    description="Create a vector region from a coarse point outline. "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "The engine fits smooth Bézier curves to your points. "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "⚠️ Coordinates MUST be in normalized 0.0–1.0 space "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "((0,0)=top-left, (1,1)=bottom-right). Passing pixel "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "coordinates (0-1000) will be rejected with an error. "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "Edit incrementally: add regions here, use style_objects "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "to recolor, delete_region to remove — never rebuild "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "the whole document from scratch. "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "📏 Proportion tip: an object's canvas footprint must "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "match its real-world size. A desk is ~90%+ of canvas "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "width; headphones on it are ~15–25%; a bottle ~6–8%. "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "If it sits on a surface, its width ≤ ¼ of the surface. "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "See resource avge://skill/design-guidelines Rule 5. "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                + SMOOTHNESS_GUIDANCE.strip()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                + " For filled shapes, prefer two-tone shading "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "(a base fill + a darker adjacent region along one edge) "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "over a single flat fill to imply depth. See resource "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "avge://skill/design-guidelines for full conventions.",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def create_region(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    outline: list[list[float]],
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    region_id: str | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    layer: str = "default",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    closed: bool = True,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    smoothness: float = 0.5,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    fill: str | None = "#CCCCCC",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    stroke: str | None = "#333333",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    stroke_width: float = 0.005,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    opacity: float = 1.0,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    fill_gradient: str | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────) -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Create a vector region from a coarse point outline.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    Args:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        outline: List of [x, y] pairs in normalized space (0.0–1.0).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            ⚠️ NOT pixel coordinates. (0,0)=top-left, (1,1)=bottom-right.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            Keep to 3–30 points for best quality.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        region_id: Optional unique ID (auto-generated if omitted).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        layer: Layer name (default "default").
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        closed: Whether shape is closed (True) or open path (False).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        smoothness: 0.0–1.0. See system prompt for per-category guidance.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        fill: Fill hex color, or omit for no fill.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        stroke: Stroke hex color, or omit for no stroke.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        stroke_width: Stroke width in normalized units.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        opacity: Object opacity 0.0–1.0.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    scene = _get_graph()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        doc_id = _resolve_doc(document_id)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except RuntimeError:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return "Error: No active document. Call create_document first."
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if not outline or len(outline) < 2:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return "Error: Outline must have at least 2 points"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if len(outline) > 200:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return (
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            f"Error: outline has {len(outline)} points "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            f"(max 200 for M0b; consider fewer points + smoothness constraints)"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        )
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    # Pass raw outline — normalize_outline validates range internally
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    norm_outline = [(float(p[0]), float(p[1])) for p in outline]
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    import json as _json
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    _tensions = None
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if smoothness_per_point:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            _tensions = _json.loads(smoothness_per_point)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            if not isinstance(_tensions, list):
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                return "Error: smoothness_per_point must be a JSON array"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        except _json.JSONDecodeError:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            return f"Error: invalid smoothness_per_point JSON: {smoothness_per_point}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    constraints = CurveConstraints(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        smoothness=max(0.0, min(1.0, smoothness)),
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        closed=closed,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        tensions=_tensions,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    )
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        resolved_fill = fill
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if fill_gradient:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        import json
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            resolved_fill = json.loads(fill_gradient)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        except json.JSONDecodeError:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            return f"Error: invalid fill_gradient JSON: {fill_gradient}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    style = Style(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        fill=None if resolved_fill is None or resolved_fill == "none" else resolved_fill,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        stroke=None if stroke is None or stroke == "none" else stroke,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        stroke_width=max(0.001, min(0.1, stroke_width)),
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        opacity=max(0.0, min(1.0, opacity)),
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    )
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        region = scene.create_region(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            outline=norm_outline,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            region_id=region_id,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            document_id=doc_id,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            layer=layer,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            z_index=z_index,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            constraints=constraints,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            style=style,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        )
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except (ValueError, RuntimeError) as e:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return f"Error: {e}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    bounds = compute_bounds(region.outline)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    bounds_str = (
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        f"x={bounds['x']:.4f} y={bounds['y']:.4f} "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        f"w={bounds['w']:.4f} h={bounds['h']:.4f}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        if bounds
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        else "N/A"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    )
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    advisory = ""
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if len(outline) > 30:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        advisory = (
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            f" Advisory: {len(outline)} points is high; "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            f"use fewer points + smoothness constraints for better curve quality."
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        )
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    return (
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        f"Region created: id={region.id}, layer={region.layer}, "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        f"bounds=({bounds_str}), points={len(outline)}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        f"{advisory}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    )
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Tool 3: style_objects ─────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────@mcp.tool(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    name="style_objects",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    description="Update the visual style of existing regions. "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "Only provided parameters are changed; omit to leave untouched. "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "Vary stroke width by hierarchy — heavier for outer silhouette, "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "lighter for internal detail — rather than using one width "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "throughout. See resource avge://skill/design-guidelines "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "for full conventions.",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def style_objects(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    ids: list[str],
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    document_id: str | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    fill: str | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    stroke: str | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    stroke_width: float | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    opacity: float | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────) -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Update the visual style of one or more existing regions.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    Args:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        ids: List of region IDs to restyle.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        fill: Fill hex color, or empty string for no fill.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        stroke: Stroke hex color, or empty string for no stroke.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        stroke_width: Stroke width in normalized units.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        opacity: Object opacity 0.0–1.0.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    scene = _get_graph()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        doc_id = _resolve_doc(document_id)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except RuntimeError:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return "Error: No active document"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    # Resolve "none" sentinels
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    resolved_fill = None if fill is None or fill == "none" else fill
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if fill_gradient:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        import json
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            resolved_fill = json.loads(fill_gradient)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        except json.JSONDecodeError:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            return f"Error: invalid fill_gradient JSON: {fill_gradient}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    resolved_stroke = None if stroke is None or stroke == "none" else stroke
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    resolved_sw = max(0.001, min(0.1, stroke_width)) if stroke_width is not None else None
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    resolved_op = max(0.0, min(1.0, opacity)) if opacity is not None else None
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        affected = scene.style_objects(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            ids=ids,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            document_id=doc_id,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            fill=resolved_fill,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            stroke=resolved_stroke,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            stroke_width=resolved_sw,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            opacity=resolved_op,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        )
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except RuntimeError as e:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return f"Error: {e}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if not affected:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return "No matching regions found"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    return f"Styled {len(affected)} region(s): {', '.join(affected)}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Tool 4: describe_scene ──────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────@mcp.tool(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    name="describe_scene",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    description="Get a text description of the current canvas — object list, "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "bounds, styles, and warnings. Use this for structural feedback.",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def describe_scene(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    detail: str = "summary",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    filter_layer: str | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    document_id: str | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────) -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Get a text description of the current canvas.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    Args:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        detail: "summary" (default) or "full" (includes outline coordinates).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        filter_layer: Optional — only describe objects in this layer.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        document_id: Document UUID (omit to use active document).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    scene = _get_graph()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        doc_id = _resolve_doc(document_id)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except RuntimeError:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return "Error: No active document — call create_document first"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    desc = scene.describe_scene(detail=detail, filter_layer=filter_layer, document_id=doc_id)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    lines: list[str] = []
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    d = desc["document"]
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    lines.append(f"Document: {d['id']}  |  Version: {d['version']}")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    lines.append(f"Canvas: {d['width']}x{d['height']} {d['unit']}, bg={d['background']}")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    lines.append(f"Regions: {desc['region_count']}")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    lines.append("")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if not desc["regions"]:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        lines.append("(No regions on canvas)")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    else:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        for r in desc["regions"]:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            b = r.get("bounds")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            bs = f"x={b['x']:.4f} y={b['y']:.4f} w={b['w']:.4f} h={b['h']:.4f}" if b else "no bounds"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            lines.append(f"  [{r['id']}] type={r['type']} layer={r['layer']}")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            lines.append(f"    Bounds: {bs}")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            lines.append(f"    Points: {r['outline_point_count']} | "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                         f"{'closed' if r['closed'] else 'open'} | "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                         f"smoothness={r['smoothness']}")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            lines.append(f"    Style: fill={r['style']['fill']} "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                         f"stroke={r['style']['stroke']} "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                         f"width={r['style']['stroke_width']}")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            lines.append("")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if desc.get("warnings"):
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        lines.append("Warnings:")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        for w in desc["warnings"]:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            lines.append(f"  ⚠ {w}")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        lines.append("")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    return "\n".join(lines)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Tool 5: render_preview ─────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────@mcp.tool(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    name="render_preview",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    description="Get a visual PNG preview of the current canvas as "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "a base64-encoded image. Use this to visually inspect your work.",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def render_preview(scale: float = 1.0, document_id: str | None = None) -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Render a visual PNG preview of the current canvas.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    Args:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        scale: Render scale factor (0.25–2.0, default 1.0).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        document_id: Document UUID (omit to use active document).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    scene = _get_graph()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        doc_id = _resolve_doc(document_id)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except RuntimeError:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return "Error: No active document — call create_document first"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    svg = svg_serialize(scene)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    scale = max(0.25, min(2.0, scale))
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        b64 = render_preview_base64(svg, scale=scale)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except RuntimeError as e:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return f"Error rendering preview: {e}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    return f"data:image/png;base64,{b64}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Tool 6: delete_region ──────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────@mcp.tool(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    name="delete_region",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    description="Delete one or more regions by ID. Returns list of "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "actually removed IDs. Use this to clean up stray geometry, "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "botched outlines, or elements you want to replace.",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def delete_region(ids: list[str], document_id: str | None = None) -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Delete one or more regions by ID.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    Args:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        ids: List of region IDs to delete (e.g. ["tag", "steam1"]).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    scene = _get_graph()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        doc_id = _resolve_doc(document_id)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except RuntimeError:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return "Error: No active document"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    deleted = scene.delete_regions(ids, doc_id)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if not deleted:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return "No matching regions found to delete"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    return f"Deleted {len(deleted)} region(s): {', '.join(deleted)}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Tool 7: edit_region ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────@mcp.tool(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    name="edit_region",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    description="Modify an existing region\'s outline, smoothness, style, or z_index. "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "Only provided fields are changed; omitted fields keep their values. "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "This is the primary refinement tool — use it to nudge existing shapes "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "rather than deleting and recreating them.",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def edit_region(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    region_id: str,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    document_id: str | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    outline: list[list[float]] | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    smoothness: float | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    smoothness_per_point: str | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    fill: str | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    stroke: str | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    stroke_width: float | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    opacity: float | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    z_index: int | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    layer: str | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────) -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Modify an existing region's properties.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    Args:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        region_id: ID of the region to edit.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        document_id: Document UUID (omit for active doc).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        outline: New outline coordinates (omit to keep current).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        smoothness: New smoothness value (omit to keep current).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        smoothness_per_point: JSON array of per-vertex tensions.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        fill: New fill hex color or gradient (omit to keep current).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        stroke: New stroke color.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        stroke_width: New stroke width.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        opacity: New opacity.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        z_index: New paint order (higher = on top).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        layer: New layer name.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    scene = _get_graph()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        doc_id = _resolve_doc(document_id)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except RuntimeError:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return "Error: No active document"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        import json as _j
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        tensions = None
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        if smoothness_per_point:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                tensions = tuple(_j.loads(smoothness_per_point))
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            except _j.JSONDecodeError:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                return f"Error: invalid smoothness_per_point JSON"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        scene.edit_region(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            region_id=region_id,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            document_id=doc_id,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            outline=outline,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            smoothness=smoothness,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            tensions=tensions,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            fill=fill,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            stroke=stroke,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            stroke_width=stroke_width,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            opacity=opacity,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            z_index=z_index,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            layer=layer,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        )
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return f"Region '{region_id}' updated"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except (ValueError, RuntimeError) as e:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return f"Error: {e}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Tool 8: duplicate_region ──────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────@mcp.tool(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    name="duplicate_region",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    description="Duplicate an existing region with optional offset and recolor. "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "Use this for highlights and shadow slivers — clone the base "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "shape, offset it slightly, and change its fill/opacity — "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "instead of authoring a new outline from scratch.",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def duplicate_region(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    region_id: str,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    new_region_id: str | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    document_id: str | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    offset_x: float = 0.0,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    offset_y: float = 0.0,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    fill: str | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    z_index: int | None = None,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────) -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Duplicate a region with optional offset and overrides.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    Args:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        region_id: ID of the region to duplicate.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        new_region_id: ID for the copy (auto-generated if omitted).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        document_id: Document UUID (omit for active doc).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        offset_x: X offset for the copy in normalized units.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        offset_y: Y offset for the copy in normalized units.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        fill: Override fill color for the copy.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        z_index: Z-index for the copy (defaults to original + 1).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    scene = _get_graph()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        doc_id = _resolve_doc(document_id)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except RuntimeError:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return "Error: No active document"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        dup = scene.duplicate_region(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            region_id=region_id,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            new_region_id=new_region_id,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            document_id=doc_id,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            offset_x=offset_x,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            offset_y=offset_y,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            fill=fill,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            z_index=z_index,
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        )
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return (
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            f"Duplicated '{region_id}' as '{dup.id}' "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            f"(offset={offset_x},{offset_y})"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        )
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except (ValueError, RuntimeError) as e:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return f"Error: {e}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Tool 9: batch ────────────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────@mcp.tool(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    name="batch",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    description="Execute multiple operations in a single call. Each op is a dict "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "with a \'tool\' key. Supported: create_region, edit_region, "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "duplicate_region, delete_region, style_objects.",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def batch(ops: list[dict], document_id: str | None = None) -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Execute multiple operations in one call.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    Args:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        ops: List of operation dicts. Each must have a "tool" key.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        document_id: Document UUID (omit for active doc).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    scene = _get_graph()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        doc_id = _resolve_doc(document_id)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except RuntimeError:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return "Error: No active document"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    results = scene.batch(ops, doc_id)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    ok = sum(1 for r in results if r["status"] == "ok")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    err = sum(1 for r in results if r["status"] == "error")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    lines = [f"Batch: {ok} ok, {err} errors"]
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    for i, r in enumerate(results):
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        status = chr(10003) if r["status"] == "ok" else chr(10007)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        msg = r.get("message", r.get("region_id", ""))
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        lines.append(f"  {status} [{i}] {msg}")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    return "\n".join(lines)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Tool 10: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────@mcp.tool(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    name="checkpoint",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    description="Save a snapshot of the current document state. "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "Use restore() to revert back to this point.",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def checkpoint(name: str = "default") -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Save a checkpoint of the current document.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    Args:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        name: Checkpoint name (default "default"). Use distinct names
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────              for multiple snapshots.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    scene = _get_graph()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if not scene.has_document():
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return "Error: No document — create one first"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    scene.checkpoint(name)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    regions = scene.region_count()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    return f"Checkpoint '{name}' saved ({regions} regions at version {scene.document.version})"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Tool 8: restore ───────────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────@mcp.tool(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    name="restore",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    description="Restore document state from a named checkpoint. "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "All changes made after the checkpoint are discarded.",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def restore(name: str = "default") -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Restore document state from a checkpoint.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    Args:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        name: Checkpoint name to restore from (default "default").
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    scene = _get_graph()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if not scene.has_document():
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return "Error: No document"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if not scene.restore(name):
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return f"Error: Checkpoint '{name}' not found. Available: {scene.list_checkpoints()}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    return f"Restored from checkpoint '{name}' ({scene.region_count()} regions at version {scene.document.version})"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Tool 9: get_document ──────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────@mcp.tool(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    name="get_document",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    description="Get metadata and a render preview URL for a document. ""
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "Returns document ID, name, size, region count, and a ""
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "base64 PNG preview. Omit document_id to use the active document.",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def get_document(document_id: str | None = None) -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Get document metadata and preview.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    Args:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        document_id: Document UUID (omit to use the active document).
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    scene = _get_graph()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        doc_id = _resolve_doc(document_id)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except RuntimeError:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return "Error: No active document. Call create_document first."
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        desc = scene.describe_scene(doc_id)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except ValueError as e:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return f"Error: {e}"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    # Render preview
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    import base64
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    try:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        from avge_engine.renderer import svg_serialize, render_preview_base64
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        svg = svg_serialize(scene)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        preview = render_preview_base64(svg, scale=0.5)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    except Exception as e:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        preview = f"(preview error: {e})"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    doc = desc["document"]
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    lines = [
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        f"Document: {doc['id']}",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        f"Name: {doc.get('name', '') or '(unnamed)'}",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        f"Canvas: {doc['width']}x{doc['height']}",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        f"Background: {doc['background']}",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        f"Regions: {desc['region_count']}",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        f"Version: {doc['version']}",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        f"Preview: {preview[:80]}...",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    ]
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    return "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────".join(lines)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Tool 10: export_svg ────────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────@mcp.tool(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    name="export_svg",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    description="Export the current canvas to an SVG file on disk. "
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────                "Returns the file path and SVG character count.",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def export_svg(
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    filepath: str = "output/scene.svg",
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────) -> str:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Export the current canvas as an SVG file on disk.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    Args:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        filepath: Path to save the SVG file (default "output/scene.svg").
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────            Relative paths are resolved from the project root.
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    scene = _get_graph()
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    if not scene.has_document():
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────        return "Error: No document — call create_document first"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    svg = svg_serialize(scene)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    from pathlib import Path
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    path = Path(filepath)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    path.parent.mkdir(parents=True, exist_ok=True)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    path.write_text(svg)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    return f"SVG saved: {path.resolve()} ({len(svg)} chars)"
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────# ── Entry ───────────────────────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def run_stdio() -> None:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Run MCP server via stdio transport."""
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    mcp.run(transport="stdio")
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────def run_sse(host: str = "0.0.0.0", port: int = 8001) -> None:
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    """Run MCP server via SSE transport (HTTP)."""
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────    mcp.run(transport="sse", host=host, port=port)
# ── Tool 10: get_history ──────────────────────────────────────────

@mcp.tool(
    name="get_history",
    description="Show mutation history for a document. Requires document_id "
                "(get from create_document or list_documents). Returns auto-saved "
                "checkpoints with timestamps and actions. Use restore() "
                "to revert to any point.",
)
def get_history(document_id: str | None = None, limit: int = 20) -> str:
    """Show mutation history for a document.

    Args:
        document_id: Document UUID (omit for active doc).
        limit: Max entries to show (default 20).
    """
    scene = _get_graph()
    try:
        doc_id = _resolve_doc(document_id)
    except RuntimeError:
        return "Error: No active document"

    history = scene.checkpoint_history(doc_id)
    if not history:
        return "(no history)"

    lines = [f"History for {doc_id} ({len(history)} entries):"]
    for entry in history[:limit]:
        name = entry.get("name", "?")
        ts = entry.get("time", "?")
        action = entry.get("action", "?")
        detail = entry.get("detail", "")
        rc = entry.get("region_count", "?")
        lines.append(f"  {name} [{ts}] {action} {detail}  ({rc} regions)")
    if history:
        lines.append(f"Use restore(name=\"{history[0].get('name','')}\") to go back.")
    return "\n".join(lines)


# ── Tool 11: checkpoint ───────────────────────────────────────────