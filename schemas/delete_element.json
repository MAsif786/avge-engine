{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://avge.dev/schemas/delete_element/v1.json",
  "title": "delete_element",
  "description": "Delete one or more elements by ID. Returns the list of actually deleted IDs.",
  "type": "object",
  "properties": {
    "ids": {
      "type": "array",
      "description": "List of element IDs to delete.",
      "items": { "type": "string" },
      "minItems": 1,
      "maxItems": 200
    }
  },
  "required": ["ids"],
  "additionalProperties": false
}
