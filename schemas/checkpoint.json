{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://avge.dev/schemas/checkpoint/v1.json",
  "title": "checkpoint",
  "description": "Save a snapshot of the current document state. The checkpoint deep-copies all elements so it survives subsequent edits.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Checkpoint name (default \"default\"). Use distinct names for multiple snapshots.",
      "default": "default"
    }
  },
  "additionalProperties": false
}
