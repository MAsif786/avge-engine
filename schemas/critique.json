{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://avge.dev/schemas/critique/v1.json",
  "title": "critique",
  "description": "Run scene critique checks. mode='rules' checks mechanical design-rule issues; mode='visual' checks preview-quality issues; mode='both' returns both.",
  "type": "object",
  "properties": {
    "document_id": {
      "type": ["string", "null"],
      "description": "Document UUID. Omit/null to use the active document."
    },
    "mode": {
      "type": "string",
      "enum": ["rules", "visual", "both"],
      "default": "both"
    },
    "min_confidence": {
      "type": "number",
      "description": "Hide visual findings below this confidence threshold.",
      "minimum": 0.0,
      "maximum": 1.0,
      "default": 0.0
    },
    "as_json": {
      "type": "boolean",
      "description": "Return machine-readable JSON instead of formatted text.",
      "default": false
    }
  },
  "additionalProperties": false
}
