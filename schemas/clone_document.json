{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://avge.dev/schemas/clone_document/v1.json",
  "title": "clone_document",
  "description": "Clone an existing document into a new document ID, including metadata, gradients, elements, layers, groups, and editable geometry.",
  "type": "object",
  "properties": {
    "source_document_id": {
      "type": ["string", "null"],
      "description": "Source document UUID. Omit/null to clone the active document."
    },
    "name": {
      "type": ["string", "null"],
      "description": "Optional name for the cloned document.",
      "maxLength": 128
    },
    "set_active": {
      "type": "boolean",
      "description": "If true, make the clone the active document.",
      "default": true
    }
  },
  "additionalProperties": false
}
